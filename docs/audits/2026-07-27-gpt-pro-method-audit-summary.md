# GPT Pro bounded methodology audit

Date: 2026-07-27
Mode: visible ChatGPT `Pro` control
Decision: `PRO_METHOD_AUDIT=ACCEPT`
C/H/M/L: `0/0/1/2`

GPT Pro accepted the public methodology and reported state for the exact
fingerprinted packet. It agreed that:

- `0/1,548 certified complete` is honest only when it is explicitly defined as
  failure to close the required evidence chain, not proof that all 1,548
  clauses have a missing version;
- official `通則` and project-assigned `chapter:00` must remain separate, and
  the reader must never render the code as an official “第 0 章”;
- date annotations are a gap-audit index, not history reconstruction;
- 2,034/3,080, 909/3,010, 490 unique, 419 ambiguous, and 0 unmapped are
  conditional candidate-search results, not verified amendment-event counts;
- all ODT/ODS/PDF attachment observations must remain non-promotable until a
  governed, independently replayable full-document verifier and separately
  reviewed admission path exist.

The one Medium finding is the deliberate absence of a positive canonical
promotion lane. Live apply, publication, and a complete-history claim remain
blocked. The two Low findings are reader/API safeguards: prevent any
`chapter:00` → “第 0 章” rendering, and always publish a numerator together
with its population definition, exclusions, and claim limit.

Exact prompt, response hashes, timestamps, URL, mode, and state fingerprint are
recorded in
`docs/audits/2026-07-27-gpt-pro-method-audit-receipt.json`. The exact UTF-8
response is preserved as deterministic gzip bytes encoded in base64.
